{% extends 'base.html.twig' %}

{% block title%} JAWEK {% endblock %}
{%block Stylesheet%}
     {{ parent() }}
     <link rel="stylesheet" href="{{ asset('assets/css/styleActivities.css') }}"/>

{% endblock %}

{% block body %}
    <div class="container">

        <h1 class="cent">Pick a Type</h1>
        <a class="btn btn-lg btn-primary halfs" href="{{ path('account.activities.event') }}">Event</a>
        <a class="btn btn-lg btn-primary halfs"  href="{{ path('account.activities.indoor') }}">Indoor</a>
        <a class="btn btn-lg btn-primary halfs"  href="{{ path('account.activities.places') }}">Place</a>


    </div>
{% endblock %}